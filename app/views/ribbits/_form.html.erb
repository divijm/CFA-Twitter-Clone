<%= form_for(ribbit) do |f| %>
  <% if ribbit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ribbit.errors.count, "error") %> prohibited this ribbit from being saved:</h2>

      <ul>
      <% ribbit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :croak %>
    <%= f.text_area :croak %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
